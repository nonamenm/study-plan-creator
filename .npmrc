# Force use of public npm registry only - no company registries
registry=https://registry.npmjs.org/
legacy-peer-deps=true
engine-strict=false
prefer-offline=false

# Explicitly disable any scoped registries that might point to company repos
@sfdc-internal:registry=https://registry.npmjs.org/
@salesforce:registry=https://registry.npmjs.org/
